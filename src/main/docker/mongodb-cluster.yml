version: '2'
services:
    jhipsterreactschool-mongodb:
        image: mongo:4.0.2
        ports:
            - "27017:27017"
        command: mongos --configdb jhipsterreactschool-mongodb-config
    jhipsterreactschool-mongodb-node:
        build:
            context: .
            dockerfile: mongodb/MongoDB.Dockerfile
        command: mongod --replSet rs1 --noprealloc --smallfiles
    jhipsterreactschool-mongodb-config:
        image: mongo:4.0.2
        container_name: jhipsterreactschool-mongodb-config
        command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
