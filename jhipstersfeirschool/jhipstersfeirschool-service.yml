apiVersion: v1
kind: Service
metadata:
    name: jhipstersfeirschool
    namespace: default
    labels:
        app: jhipstersfeirschool
spec:
    selector:
        app: jhipstersfeirschool
    type: LoadBalancer
    ports:
        - name: http
          port: 8080
